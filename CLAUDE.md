# GridTabs Picks（网站集数据库）

## 子模块说明

网站集分享数据库，用于：
1. GridTabs 官网展示精选网站合集
2. 生成社交媒体分享内容（Twitter、Reddit、Threads）
3. 生成短视频脚本（TikTok、小红书）

## 自动化管道

```
/urldiscover → /collect → /curate → /seo-review → /copy-review → /publish-pick
   广泛收集      手动补充     组装主题    SEO检查       文案检查        发布
                                                                      ↓
                         /twitter-post  /threads-post  /reddit-post
                         /tiktok-script  /xiaohongshu-post
```

## 文件结构

```
picks/
├── picks.json              # 🔒 已发布，官网直接引用
├── images/                 # 🔒 已发布，官网直接引用
├── inbox.md                # 网站收集池（Markdown，人可读）
├── sites/                  # 网站详情档案（每站一个 .md）
├── drafts/                 # 草稿 picks（.md 格式，发布时转 JSON）
├── content/                # 生成的营销内容
│   └── {slug}/
│       ├── twitter.md
│       ├── threads.md
│       ├── reddit.md
│       ├── tiktok.md
│       └── xiaohongshu.md
└── CLAUDE.md
```

### 文件职责

| 文件 | 说明 | 谁写 |
|------|------|------|
| `picks.json` | 已发布合集，官网读取，不要手动改 | `/publish-pick` |
| `inbox.md` | 网站池，一张表快速浏览，不含 tag | `/collect` `/urldiscover` |
| `sites/*.md` | 网站深度档案（功能、视觉、氛围等） | `/curate` Phase 1 |
| `drafts/*.md` | 草稿合集，人可读可编辑 | `/curate` Phase 3 |
| `content/{slug}/*` | 各平台分发内容 | 内容生成 skills |

## 数据结构

主数据文件：`picks.json`

```typescript
interface Pick {
  slug: string;
  title: { en: string; "zh-CN": string };
  description: { en: string; "zh-CN": string };
  date: string;
  coverImage: string;
  sites: Site[];
}

interface Site {
  name: string;
  url: string;
  description: { en: string; "zh-CN": string };
}
```

## 内容规范

- 每个 Pick 包含 8-10 个网站
- 标题要有吸引力，避免 AI 味
- 描述要具体，说明为什么值得关注
- 中英文都要自然，不是直译
- 一个网站可以出现在多个 picks 中

## 当前状态

- 已有 2 个 picks（已发布）
- inbox 有 37 个网站
- 管道已搭建完成，进入使用阶段

## Skills 一览

### 收集阶段

| Skill | 类型 | 用途 |
|-------|------|------|
| `/collect` | skill | 手动给 URL，自动补全信息，追加到 inbox.md |
| `/urldiscover` | agent | 广泛搜索 HN/PH/Reddit/Twitter 等，批量收集热门网站 |

### 组装阶段

| Skill | 类型 | 用途 |
|-------|------|------|
| `/curate` | agent | 分析网站详情 → 建档 sites/ → 提出主题 → 组装 draft |

### 审核阶段

| Skill | 类型 | 用途 |
|-------|------|------|
| `/seo-review` | skill | 检查标题/描述/slug/关键词，输出评分和优化建议 |
| `/copy-review` | skill | 检查中英文自然度、AI 味、品牌语气 |

### 发布阶段

| Skill | 类型 | 用途 |
|-------|------|------|
| `/publish-pick` | skill | 解析 draft md → JSON → 写入 picks.json（需确认） |

### 内容分发

| Skill | 平台 | 语言 | 输出 |
|-------|------|------|------|
| `/twitter-post` | Twitter/X | 英文 | 3 版本（清单/故事/观点） |
| `/threads-post` | Threads | 英文 | 2 版本，短内容无外链 |
| `/reddit-post` | Reddit | 英文 | 含 subreddit 推荐 |
| `/tiktok-script` | TikTok | 英文 | 分镜脚本带时间轴 |
| `/xiaohongshu-post` | 小红书 | 中文 | 标题+正文+标签+封面建议 |
